import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ActualizardisponibilidadPage } from './actualizardisponibilidad.page';

describe('ActualizardisponibilidadPage', () => {
  let component: ActualizardisponibilidadPage;
  let fixture: ComponentFixture<ActualizardisponibilidadPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ActualizardisponibilidadPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
